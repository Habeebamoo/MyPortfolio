export interface Project {
  slug: string,
  name: string,
  description: string,
  stack: string[],
  about: string,
  url: string,
  repo: string,
  images: string[],
  createdAt: string,
  updatedAt: string
}